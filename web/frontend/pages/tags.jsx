import { useNavigate } from "react-router-dom";
import { Button, Card, Layout, Page, Stack, Text, TextField } from "@shopify/polaris";
import { TitleBar } from "@shopify/app-bridge-react";
export default function Tags() { const navigate = useNavigate(); return <Page backAction={{ content: "Dashboard", onAction: () => navigate("/") }} fullWidth><TitleBar title="Tag manager" primaryAction={{ content: "Apply tags", onAction: () => {} }} /><Layout><Layout.Section><div style={{ marginTop: 20 }}><Card sectioned><Stack vertical><TextField label="Find a tag" labelHidden placeholder="Search tags" autoComplete="off" /><Text as="p">Use bulk operations to apply, remove, merge, or clean up product tags.</Text><Button>Review duplicate tags</Button></Stack></Card></div></Layout.Section></Layout></Page>; }
